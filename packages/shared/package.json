{
  "name": "@bunny2/shared",
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'shared is consumed as source via Bun workspaces — no build needed' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}
