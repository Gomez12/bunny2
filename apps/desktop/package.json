{
  "name": "@bunny2/desktop",
  "private": true,
  "type": "commonjs",
  "version": "0.0.0",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p .",
    "dev": "bun run scripts/dev.ts",
    "electron:dev": "electron .",
    "typecheck": "tsc -p tsconfig.checks.json",
    "test": "bun test",
    "fetch-bun": "bun run scripts/fetch-bun-runtimes.ts",
    "package": "electron-builder --publish=never"
  },
  "devDependencies": {
    "@types/node": "^20.12.0",
    "electron": "^32.0.0",
    "electron-builder": "^25.1.8"
  }
}
